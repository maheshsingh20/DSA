#include <bits/stdc++.h>
using namespace std;

int main(){
  ios_base::sync_with_stdio(0);
  cin.tie(0);
  vector<int> arr = {1, 2, 3, 4, 4, 5, 5, 6, 2};
  int ans = 1;
  for (int i = 0; i < arr.size();i++){
    
  }
}