#include <bits/stdc++.h>
using namespace std;


int main(){
  ios_base::sync_with_stdio(nullptr);
  cin.tie(0);
  cout.tie(0);
  int days;
  cin >> days;
  vector<int> works = {8, 6, 7, 4, 3, 45, 9, 2};
  int n = works.size();
  vector<vector<int>> t(n + 1, vector<int>(n + 1, 0));
  for (int i = 0; i <= n;i++){
    for (int j = i; j <= n;j++){
      
    }
  }
}
