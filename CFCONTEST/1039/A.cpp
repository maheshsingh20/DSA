#include <bits/stdc++.h>
using namespace std;
#define int long long


int32_t main(){
   ios_base::sync_with_stdio(false);
   cin.tie(nullptr);
   int t;
   while(t--){
      int n, c;
      cin >> n >> c;
      vector<int> arr(n);
      for (int i = 0; i < n; i++){
         cin >> arr[i];
      }
      
   }
}