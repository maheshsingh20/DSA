#include <bits/stdc++.h>
using namespace std;


int main(){
  ios_base::sync_with_stdio(false);
  cin.tie(NULL);
  string str;
  cin >> str;
  string t = "$#";
  for(auto ch:str){
    t += ch;
    t += "#";
  }
  int n = t.length();
  vector<int> p(n);
  int c = 0;
  int r = 0;
  for (int i = 0;i<)
}
